import random

r = random.randrange(-5,10) 
# rand range would not include upper bound value in output


p = random.randint(1, 6) 
# rand int would include upper bound value in outputs


q = random.randrange(5) 
# rand range can also be used like this

print(r)
print(p)